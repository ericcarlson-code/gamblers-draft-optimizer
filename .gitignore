# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
.conda-env/
.pytest_cache/
*.egg-info/

# Streamlit
.streamlit/secrets.toml

# Data (player projections/CSVs may contain licensed data - keep local)
/data/uploads/
*.csv
!data/sample/*.csv
!data/historical/*.csv

# Downloaded image cache (regenerable via scripts/fetch_player_images.py,
# embedded as base64 into the site build -- not the raw source of truth)
/data/image_cache/

# Node (kept in case of future React frontend)
node_modules/
dist/
build/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Env
.env

# Yahoo OAuth app credentials and tokens -- never commit these
.credentials/
