{
  "league": {
    "name": "The Gamblers 2025",
    "yahoo_league_id": "732599",
    "num_teams": 10,
    "platform": "Yahoo",
    "draft_type": "offline",
    "scoring_type": "season points"
  },
  "tiering": {
    "gap_threshold_stdevs": 1.0,
    "note": "Within a position, a new tier starts wherever the point drop-off to the next-best player exceeds (average drop-off + this many standard deviations of drop-off). Raise this to get fewer/bigger tiers, lower it for more/smaller tiers."
  },
  "season": {
    "games_per_season": 17,
    "start_date": "2026-09-03",
    "note": "Used to convert a defense's season-total points allowed into a per-game average for the points_allowed_buckets lookup, then scale the bucket result back up across the season. start_date (approximate NFL Week 1 kickoff) seeds the site's default 'current week' for This Week / Rest of Season views -- users can override it directly in League Details, this is just a starting guess."
  },
  "roster": {
    "slots": {
      "QB": 2,
      "WR": 4,
      "RB": 3,
      "TE": 1,
      "K": 2,
      "DEF": 1,
      "BN": 1
    },
    "note": "Starter counts per position (BN excluded from replacement-level math since it can hold any position)."
  },
  "scarcity_boosts": {
    "QB": { "max_multiplier": 1.25, "taper_to_rank": 8 },
    "note": "Real historical draft behavior for this league shows an early QB run that flat VOR-over-replacement doesn't capture on its own -- 2024's real draft had 7 QBs in the first 20 picks (10 by pick 30), 2025's had 9 in the first 20 (11 by pick 30), and real 2QB-format ADP shows the same pattern (e.g. Lamar Jackson 4.7, Joe Burrow 4.3) even though the model's own pre-boost VOR ranked them #21/#31 overall. This multiplies a position's VOR by a factor that decays linearly from max_multiplier at that position's #1-ranked player down to 1.0x at taper_to_rank (and stays 1.0x beyond it) -- VOR remains the sole ranking mechanism (this boosts VOR itself, doesn't blend in ADP), it just corrects for a positional-run/scarcity dynamic that a static per-position replacement baseline doesn't model. Recalibrate these two numbers (or add another position, if real evidence ever supports it) without a code change. CALIBRATED 2026-07-29 against an actual simulated Mock Draft (bots draft by this same VOR, see site/rankings_template.html's runBotPicksUntilMe), not just the static Rankings order -- the original 1.9/16 pair (chosen only by eyeballing the Rankings list) produced an 18-QBs-in-20-picks run once bots actually drafted by it, far more extreme than real evidence anywhere. Re-tuned against the full available evidence: this league's own 2024/2025 drafts (7 and 9 QBs in the first 20 picks) AND the broader real FFC 2QB-format ADP archive across 2020-2024 (QBs in top-20-by-ADP: 5, 7, 8, 8, 10 -- average 7.6) -- both point to ~7-8 QBs in the first 20 picks. 1.25/8 hits that (simulated average 7.7-7.8 across 50 trials). NOTE: the simulated top-30 count still runs high (~15-16 vs the ~10 real evidence suggests) regardless of this boost's calibration -- confirmed structural, not fixable by this multiplier alone: pickForBot's starter-need priority (a separate, deliberately-built feature) makes every team grab their mandatory 2nd QB starter as soon as it's still open, which drives most of the pick-21-30 QB volume on its own (a boost of exactly 1.0/no-boost-at-all still simulates to ~12.5 QBs by pick 30). Fixing that fully would mean softening starter-need-always-wins into more of a tie-break, a real design change to a feature that was explicitly requested this same session -- flagged, not done without being asked."
  },
  "scoring": {
    "passing": {
      "yards_per_point": 50,
      "touchdown": 4
    },
    "rushing": {
      "yards_per_point": 20,
      "touchdown": 6
    },
    "receiving": {
      "yards_per_point": 20,
      "touchdown": 6,
      "reception": 0
    },
    "misc": {
      "offensive_fumble_return_touchdown": 6,
      "two_point_conversion": 2,
      "note": "return_touchdown (punt/kick return TDs) is intentionally NOT here -- confirmed 2026-07-28 that a return TD is a DEFENSE-only stat (6pts, see scoring.defense.return_touchdown), not also credited to the individual returner. Previously this section also had 'return_touchdown': 6, which double-counted the same play on both the returner's own line and the DEF unit's -- optimizer/scoring.py's score_misc() no longer reads it."
    },
    "kicking": {
      "extra_point": 1,
      "field_goal_buckets": [
        { "min_yards": 0, "max_yards": 39, "points": 3 },
        { "min_yards": 40, "max_yards": 49, "points": 4 },
        { "min_yards": 50, "max_yards": 59, "points": 5 },
        { "min_yards": 60, "max_yards": 999, "points": 15 }
      ]
    },
    "defense": {
      "touchdown": 6,
      "safety": 2,
      "return_touchdown": 6,
      "extra_point_returned": 0,
      "points_allowed_buckets": [
        { "min_points": 0, "max_points": 0, "points": 15 },
        { "min_points": 1, "max_points": 6, "points": 10 },
        { "min_points": 7, "max_points": 13, "points": 7 },
        { "min_points": 14, "max_points": 20, "points": 1 },
        { "min_points": 21, "max_points": 27, "points": 0 },
        { "min_points": 28, "max_points": 34, "points": -4 },
        { "min_points": 35, "max_points": 999, "points": -7 }
      ]
    }
  },
  "notes": [
    "CONFIRMED as of 2026-07-26 against the league's real rules document (FOOTBALL_RULES_2023-2024.docx), with a direct user correction on the 60+ FG value. Passing/rushing/receiving/roster/D-ST points-allowed all matched the prior placeholder values exactly. FG buckets were corrected to add the 60+ yard tier -- worth 15pts (not the doc's written 9), a huge premium over 5pts for 50-59 -- that the placeholder didn't have. extra_point_returned corrected from a guessed 2 down to 0 -- not a scored category in the real rules.",
    "Re-confirmed 2026-07-27 (session 6): user explicitly re-verified the 60+ yard / 15pt tier stays -- it's a real differentiator that mattered to their actual payout in a real week last season, not a rounding-error edge case. Do not simplify/merge it away again without asking.",
    "The real rules also specify a 2-point-conversion rule where EVERY player involved gets 2 points (e.g. both the QB who threw it and the receiver who caught it) -- this already matches how two_point_conversion is applied per-player in the scoring engine, no change needed there.",
    "IMPORTANT data-source caveat given the 60+ tier is now worth 3x the 50-59 tier: our stats source only reports one aggregate '50+ yard' field goal make count, with no way to split out true 60+ yard makes. See optimizer/schema.py's FG_INPUT_BANDS comment -- the aggregate is conservatively treated as 50-59 yard makes (5pts) rather than 60+ (15pts) to avoid overvaluing kickers, but this means genuine 60+ specialists are currently UNDERVALUED since their bonus isn't captured at all. Worth sourcing real distance-level kicker data (e.g. per-game logs) if accuracy here matters enough -- flagged again session 6 since the user says this real gap has cost real money, so it's worth prioritizing over other polish.",
    "The rules doc is titled '2023-2024' -- worth double-checking with the user whether anything changed for the current season.",
    "Edit the bucket lists freely -- add/remove/adjust rows, the scoring engine reads them at runtime and does not assume a fixed number of buckets.",
    "Passing interceptions are not scored here because the league's INT penalty (if any) was not provided -- add a 'interception' key under scoring.passing if the real league docks points for it.",
    "Rookie projections (2026-07-26): players with zero real NFL stats (this year's incoming draft class) are projected from NFL draft capital alone -- averaged rookie-year stats of past draftees in the same round-group + position, from optimizer/rookie_projections.py. Tagged 'R' in Rankings/Mock Draft/Team Rosters. Two known limitations: (1) it's round+position only, no college production or combine data, so e.g. two round-1 WRs in the same draft class get an identical projection; (2) the training data (fetch_actuals.py) only captures rookies who ranked highly enough to appear on a stats leaderboard, so late-round baselines skew optimistic (rookies who got zero playing time are simply absent, not a real zero, so they don't drag the average down).",
    "ADP + Value vs ADP (updated 2026-07-27): 'ADP' is consensus Average Draft Position from Fantasy Football Calculator's free API (scripts/fetch_adp.py, data/historical/adp.csv), attribution: fantasyfootballcalculator.com -- specifically their '2qb' format, real empirical ADP from actual 2-QB-format drafts, since this league starts 2 QBs and a generic 1-QB PPR list would misvalue QBs badly. Their non-PPR-leaning default also better matches this league's 0-points-per-reception scoring than a PPR list would. Unlike the original ESPN-based source, FFC genuinely has historical year-specific data (2020-2024, data/historical/{year}_adp.csv) -- 2025 has no 2QB data at FFC and is skipped, and historical years default to a 12-team pool (FFC doesn't archive 10-team-specific historical drafts) rather than erroring, so treat historical ADP as directionally useful, not exact. Coverage is real-draft-sample-limited (~200 players currently have enough 2QB-format drafts at FFC to report ADP, well short of our full ~550-player pool) -- deep-bench/rookie-model players without a real ADP show as unranked rather than a fabricated number. 'Value vs ADP' compares a player's VOR to the VOR our own model has at whoever it ranks at that same ADP slot -- positive means our model likes this player more than the market does (a potential value/sleeper at their current ADP), negative means the market is drafting them ahead of where our own model would. It is NOT blended into VOR/rankings itself -- it's a separate comparison signal, so the market's opinion never silently overrides our own real-stats-driven model."
  ]
}
